#include <stdio.h>
#include <string.h>
#define BUFFER 256
int main()
{
    int num=-1;
    char* buf[256]= {'\0'};
    scanf("%d",&num);
    do
    {
        scanf("%s",&buf);
    }
    while(strlen(buf));

    ///·F!
}
