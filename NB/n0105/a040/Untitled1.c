#include <limits.h>
#include <stdio.h>
#include <math.h>

int main()
{
//printf("%d",(int)log10f(48854564)+1);
    float a=0,b=0;
    scanf("%f %f",&a,&b);
    for(float i = a + 1 ; i < b ; i++)
    {
        for(int j = 0 ; j < ((int)log10f(i)) ; j ++ )
        {


        }
    }
}
